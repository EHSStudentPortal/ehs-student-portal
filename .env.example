# Copy to .env.local and fill in values. Never commit .env.local
# Generate hash: node -e "const c=require('crypto');console.log(c.createHash('sha256').update('YourPassword').digest('hex'))"
NEXT_PUBLIC_ADMIN_PW_HASH=your_sha256_hash_here
