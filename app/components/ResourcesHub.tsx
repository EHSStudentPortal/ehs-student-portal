'use client';
export default function ResourcesHub({ onNavigate }: { onNavigate: (tab: string) => void }) {
  return <div style={{ padding:'2rem', color:'var(--text-primary)' }}>Resources Hub — loading…</div>;
}
